var dbIndex = require('../db');
var db= dbIndex.db;

import chai, {expect} from 'chai';

describe('TEST', () => {

  describe('should asdfasdf', () => {
    var a= 2
    beforeEach('asdf', () => {
    });

    it('asdf', () => {
      expect(a+a).to.be.equal(4)

    });

  });

});
